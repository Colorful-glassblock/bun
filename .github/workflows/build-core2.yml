name: Build for Core2 with SSSE3

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    name: Build (Core2 + SSSE3)
    runs-on: ubuntu-22.04
    
    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Setup Rust toolchain
      uses: dtolnay/rust-toolchain@stable
      with:
        targets: x86_64-unknown-linux-gnu

    - name: Cache dependencies
      uses: Swatinem/rust-cache@v2
      with:
        cache-on-failure: true

    - name: Install sccache (without wrapper)
      run: |
        cargo install sccache --locked --force

    - name: Cache sccache
      uses: actions/cache@v4
      with:
        path: ~/.cache/sccache
        key: ${{ runner.os }}-sccache-${{ hashFiles('Cargo.lock') }}
        restore-keys: |
          ${{ runner.os }}-sccache-

    - name: Configure Cargo
      run: |
        mkdir -p ~/.cargo
        cat > ~/.cargo/config.toml << 'EOF'
        [build]
        jobs = 4
        rustc-wrapper = "sccache"
        
        [target.x86_64-unknown-linux-gnu]
        linker = "clang"
        rustflags = [
            "-C", "target-cpu=core2",
            "-C", "target-feature=+ssse3",
            "-C", "lto=off",
            "-C", "codegen-units=8",
            "-C", "debuginfo=0",
            "-C", "opt-level=0",
            "-C", "panic=abort",
        ]
        
        [profile.release]
        opt-level = 0
        debug = false
        lto = false
        codegen-units = 8
        panic = "abort"
        incremental = false
        EOF

    - name: Build release binary
      run: |
        cargo clean
        cargo build --release --target x86_64-unknown-linux-gnu
      env:
        RUSTC_WRAPPER: sccache

    - name: Verify build artifacts
      run: |
        echo "=== Binary info ==="
        ls -lh target/x86_64-unknown-linux-gnu/release/ | grep -v "\.d" | head -10
        echo ""
        echo "=== File type ==="
        file target/x86_64-unknown-linux-gnu/release/* 2>/dev/null | grep ELF | head -3
        echo ""
        echo "=== Architecture tags ==="
        readelf -A target/x86_64-unknown-linux-gnu/release/* 2>/dev/null | head -10 || true
        echo ""
        echo "=== SSSE3 instructions (sample) ==="
        objdump -d target/x86_64-unknown-linux-gnu/release/* 2>/dev/null | grep -i ssse3 | head -10 || echo "No SSSE3 instructions found"

    - name: Upload binary artifacts
      uses: actions/upload-artifact@v4
      with:
        name: core2-ssse3-release
        path: |
          target/x86_64-unknown-linux-gnu/release/
          !target/x86_64-unknown-linux-gnu/release/*.d
        if-no-files-found: error
        retention-days: 30
